# AGENTS.md

## Project Verification

- After any file edit in this project, run the Windows Desktop build before finishing unless the user explicitly says not to.
- Use this build command:

```powershell
powershell -ExecutionPolicy Bypass -File "C:\卒業制作\tools\export_windows_desktop.ps1" -GodotPath "C:\卒業制作\tools\godot\Godot_v4.5-stable_win64_console.exe"
```

- If the build cannot be run, explain why and report the exact command that should be run next.
